## Installation

Add this line to your application's Gemfile:

```ruby
gem 'agile-cli'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install agile-cli

## Getting Started

### Installation
    ```
    $ gem install agile-cli

    ```
### Using
    ```
    $ agile

    ```
